---
phase: 03-baseline-implementations
plan: 15
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements: [BASE-02, BASE-03]

must_haves:
  truths:
    - "AMP-safe BN runs without NaN and without AttributeError (or prints a valid skip message when CUDA is unavailable)"
  artifacts:
    - path: "src/octonion/baselines/_normalization.py"
      provides: "QuaternionBatchNorm and OctonionBatchNorm with AMP-safe whitening"
      contains: "autocast"
  key_links:
    - from: "QuaternionBatchNorm / OctonionBatchNorm"
      to: "torch.amp.autocast"
      via: "BN forward runs under autocast without NaN, output dtype float16 or float32"
      pattern: "autocast.*enabled=False|float32"
---

<objective>
Verify that AMP-safe BN (QuaternionBatchNorm, OctonionBatchNorm) actually passes by re-running the exact UAT command.

Purpose: The previous UAT failure was a user invocation error — `BN.name` (no double underscores) instead of `BN.__name__`. The BN code itself is correct and has no bug. No code changes are needed. This plan re-runs the exact correct command from 03-UAT.md test 9 and records the result. If CUDA is unavailable the test prints a skip message (acceptable). The plan is marked autonomous: false because the user must run and observe the CUDA-dependent verification.

Output: Confirmed pass (or CUDA-skip) for AMP-safe BN. Gap 3 from 03-UAT.md is closed.
</objective>

<execution_context>
@/home/aescalera/.claude/get-shit-done/workflows/execute-plan.md
@/home/aescalera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the exact UAT command for AMP-safe BN</name>
  <files></files>
  <action>
    No code changes. Run the exact command from 03-UAT.md test 9, which uses BN.__name__ (with double underscores):

    ```
    docker compose run --rm dev uv run python -c "
    import torch
    from octonion.baselines import QuaternionBatchNorm, OctonionBatchNorm
    if not torch.cuda.is_available():
        print('CUDA not available, skip')
        exit(0)
    for BN, dim in [(QuaternionBatchNorm, 4), (OctonionBatchNorm, 8)]:
        bn = BN(16).cuda()
        x = torch.randn(64, 16, dim).cuda()
        with torch.amp.autocast(device_type='cuda'):
            out = bn(x)
        assert not torch.isnan(out).any(), f'{BN.__name__} produced NaN!'
        print(f'{BN.__name__}: no NaN, dtype={out.dtype} OK')
    "
    ```

    Acceptable outcomes:
    - CUDA available: prints "QuaternionBatchNorm: no NaN, dtype=... OK" and "OctonionBatchNorm: no NaN, dtype=... OK"
    - CUDA not available: prints "CUDA not available, skip"

    Both outcomes close this gap. Any other outcome (AttributeError, NaN assertion, unexpected exception) is a real bug and must be reported in the SUMMARY with full traceback — do NOT attempt to fix it here.

    Note: The previous failure was `BN.name` (missing double underscores). The command above uses `BN.__name__` which is the correct Python dunder attribute. This is purely a verification run.
  </action>
  <verify>
    <automated>docker compose run --rm dev uv run python -c "import torch; from octonion.baselines import QuaternionBatchNorm, OctonionBatchNorm; print('import OK'); cuda_ok = torch.cuda.is_available(); print(f'CUDA={cuda_ok}')" 2>&1</automated>
  </verify>
  <done>
    Command exits with code 0. Either "QuaternionBatchNorm: no NaN ... OK" and "OctonionBatchNorm: no NaN ... OK" are printed, or "CUDA not available, skip" is printed. No AttributeError, no NaN assertion failure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirms AMP-safe BN output</name>
  <action>No automated action — review the Task 1 output and confirm the result.</action>
  <what-built>
    AMP-safe BN re-verification. Task 1 ran the exact UAT command (with BN.__name__). The output was captured above.
  </what-built>
  <how-to-verify>
    Review the Task 1 output above.

    PASS if output contains either:
    - "QuaternionBatchNorm: no NaN" and "OctonionBatchNorm: no NaN" (CUDA available path)
    - "CUDA not available, skip" (no CUDA path)

    FAIL if output contains:
    - AttributeError (wrong dunder usage — report as new bug)
    - "produced NaN!" (real AMP bug — report as new bug)
    - Any unexpected exception

    If PASS: type "approved"
    If FAIL: describe the error so it can be filed as a new gap
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification required for CUDA-dependent test</automated>
  </verify>
  <done>User confirms output is acceptable (pass or CUDA-skip).</done>
  <resume-signal>Type "approved" if AMP BN verified, or describe the failure</resume-signal>
</task>

</tasks>

<verification>
- No new AttributeError (previous failure was user error, not code bug)
- AMP-safe BN either passes or is confirmed skipped due to no CUDA
- 03-UAT.md gap 3 can be marked resolved
</verification>

<success_criteria>
The AMP-safe BN UAT command runs without error. Gap 3 from 03-UAT.md is resolved. All 3 UAT gaps are now closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-baseline-implementations/03-15-SUMMARY.md`
</output>
