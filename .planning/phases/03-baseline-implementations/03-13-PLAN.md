---
phase: 03-baseline-implementations
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_perf_equivalence.py
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, BASE-03]

must_haves:
  truths:
    - "Full test suite passes with 0 failures"
  artifacts:
    - path: "tests/test_perf_equivalence.py"
      provides: "Updated test file with rows/cols arguments at all _tril_to_symmetric call sites"
      contains: "rows, cols = torch.tril_indices"
  key_links:
    - from: "tests/test_perf_equivalence.py"
      to: "src/octonion/baselines/_normalization._tril_to_symmetric"
      via: "_tril_to_symmetric(flat, dim=N, rows=rows, cols=cols)"
      pattern: "_tril_to_symmetric.*rows=rows.*cols=cols"
---

<objective>
Commit the working-tree fix to tests/test_perf_equivalence.py so the full test suite passes.

Purpose: The committed version of tests/test_perf_equivalence.py still calls _tril_to_symmetric with 2 positional arguments (flat, dim=N). The _normalization.py implementation was updated in plan 03-10 to require 4 arguments (flat, dim, rows, cols) to avoid recomputing tril_indices on every call. The working tree already contains the fix — every call site prepends `rows, cols = torch.tril_indices(N, N)` and passes rows/cols as keyword arguments. This plan stages and commits those changes.

Output: Committed test file with correct 4-argument call sites. Full test suite green.
</objective>

<execution_context>
@/home/aescalera/.claude/get-shit-done/workflows/execute-plan.md
@/home/aescalera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and commit the working-tree test fix</name>
  <files>tests/test_perf_equivalence.py</files>
  <action>
    The working-tree version of tests/test_perf_equivalence.py already contains the fix. Do NOT modify it further. Execute the following steps in order:

    1. Confirm the fix is present in the working tree:
       Run `git diff tests/test_perf_equivalence.py` on the host.
       Verify the diff shows `rows, cols = torch.tril_indices(N, N)` additions before each `_tril_to_symmetric` call, and that each call now passes `rows=rows, cols=cols`.

    2. Run the relevant test class in the container to confirm the fix works before committing:
       `docker compose run --rm dev uv run pytest tests/test_perf_equivalence.py -v 2>&1 | tail -20`
       All tests in this file must pass (0 failures).

    3. Stage only this file:
       `git add tests/test_perf_equivalence.py`

    4. Commit:
       `git commit -m "fix(tests): pass rows/cols to _tril_to_symmetric at all call sites"`

    Do NOT run `uv sync` first — the container already has project deps installed.
    Do NOT modify _normalization.py — the production code is correct, only the test needed updating.
  </action>
  <verify>
    <automated>docker compose run --rm dev uv run pytest tests/test_perf_equivalence.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `git diff tests/test_perf_equivalence.py` shows empty (clean working tree for this file)
    - `docker compose run --rm dev uv run pytest tests/test_perf_equivalence.py -v` shows 0 failed tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm full test suite is green</name>
  <files></files>
  <action>
    Run the full test suite to confirm no regressions from the committed fix:
    `docker compose run --rm dev uv run pytest 2>&1 | tail -10`

    Expected: 0 failures, 0 errors. A small number of CUDA-only tests may be skipped — that is acceptable.

    If any failures appear that are NOT in test_perf_equivalence.py, note them in the SUMMARY but do not fix them here — they belong in a separate gap closure plan.
  </action>
  <verify>
    <automated>docker compose run --rm dev uv run pytest 2>&1 | tail -5</automated>
  </verify>
  <done>Full pytest run shows 0 failures, 0 errors.</done>
</task>

</tasks>

<verification>
- `git log --oneline -1` shows the fix(tests) commit
- `docker compose run --rm dev uv run pytest tests/test_perf_equivalence.py -v` shows all pass
- `docker compose run --rm dev uv run pytest` shows 0 failures overall
</verification>

<success_criteria>
Full test suite passes with 0 failures. The _tril_to_symmetric TypeError is gone. Gap 1 from 03-UAT.md is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-baseline-implementations/03-13-SUMMARY.md`
</output>
