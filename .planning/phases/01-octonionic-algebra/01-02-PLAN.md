---
phase: 01-octonionic-algebra
plan: 02
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - src/octonion/_octonion.py
  - src/octonion/_tower.py
  - src/octonion/_random.py
  - src/octonion/__init__.py
  - tests/conftest.py
  - tests/test_octonion_class.py
  - tests/test_types.py
  - tests/test_tower.py
  - tests/test_random.py
  - tests/test_algebraic_properties.py
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Moufang identities pass on 10,000+ random octonion triples at float64 precision (ROADMAP success criterion 1)"
    - "Norm preservation |ab| = |a||b| holds to within 1e-12 relative error (ROADMAP success criterion 2)"
    - "Inverse satisfies a * a_inv = 1 and a_inv * a = 1 to numerical precision (ROADMAP success criterion 4)"
    - "Associator is non-zero for generic triples but zero when any two args are equal (ROADMAP success criterion 5)"
    - "Octonion class provides immutable wrapper with operator overloading for *, +, -, neg, eq"
    - "R/C/H types implement NormedDivisionAlgebra and pass their own algebraic property tests"
    - "Random generation produces reproducible results with seed control"
  artifacts:
    - path: "src/octonion/_octonion.py"
      provides: "Core Octonion class with all operators and methods"
      exports: ["Octonion"]
      min_lines: 100
    - path: "src/octonion/_tower.py"
      provides: "Real, Complex, Quaternion types in Cayley-Dickson tower"
      exports: ["Real", "Complex", "Quaternion"]
      min_lines: 80
    - path: "src/octonion/_random.py"
      provides: "Random octonion generation utilities"
      exports: ["random_octonion", "random_unit_octonion", "random_pure_octonion"]
      min_lines: 40
    - path: "tests/test_algebraic_properties.py"
      provides: "FOUND-01 success criteria property tests"
      min_lines: 100
    - path: "tests/test_octonion_class.py"
      provides: "Octonion class API tests"
      min_lines: 80
    - path: "tests/test_tower.py"
      provides: "R/C/H type tests"
      min_lines: 60
  key_links:
    - from: "src/octonion/_octonion.py"
      to: "src/octonion/_multiplication.py"
      via: "Octonion.__mul__ calls octonion_mul"
      pattern: "octonion_mul"
    - from: "src/octonion/_octonion.py"
      to: "src/octonion/_types.py"
      via: "Octonion extends NormedDivisionAlgebra"
      pattern: "NormedDivisionAlgebra"
    - from: "src/octonion/_tower.py"
      to: "src/octonion/_types.py"
      via: "Real/Complex/Quaternion extend NormedDivisionAlgebra"
      pattern: "NormedDivisionAlgebra"
    - from: "tests/test_algebraic_properties.py"
      to: "src/octonion/_octonion.py"
      via: "Property tests use Octonion class for Moufang, norm, inverse, alternativity"
      pattern: "Octonion"
---

<objective>
Build the Octonion class wrapper with full operator overloading, the R/C/H/O Cayley-Dickson tower types, random generation utilities, and the FOUND-01 success criteria property-based test suite (Moufang identities, norm preservation, inverse, alternativity).

Purpose: This plan delivers the user-facing API and the critical verification tests that establish trust in the algebra. The 4 of 5 ROADMAP success criteria are tested here (criterion 3, the Cayley-Dickson cross-check, was tested in Plan 01).
Output: Complete Octonion class, R/C/H tower types, random generators, and green property-based test suite proving algebraic correctness.
</objective>

<execution_context>
@/home/aescalera/.claude/get-shit-done/workflows/execute-plan.md
@/home/aescalera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-octonionic-algebra/01-CONTEXT.md
@.planning/phases/01-octonionic-algebra/01-RESEARCH.md
@.planning/phases/01-octonionic-algebra/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan builds on. -->

From src/octonion/_types.py:
```python
from abc import ABC, abstractmethod
import torch

class NormedDivisionAlgebra(ABC):
    @abstractmethod
    def conjugate(self) -> 'NormedDivisionAlgebra': ...
    @abstractmethod
    def norm(self) -> torch.Tensor: ...
    def norm_squared(self) -> torch.Tensor: ...
    @abstractmethod
    def inverse(self) -> 'NormedDivisionAlgebra': ...
    @abstractmethod
    def __mul__(self, other) -> 'NormedDivisionAlgebra': ...
    @property
    @abstractmethod
    def components(self) -> torch.Tensor: ...
    @property
    @abstractmethod
    def dim(self) -> int: ...
```

From src/octonion/_multiplication.py:
```python
STRUCTURE_CONSTANTS: torch.Tensor  # Shape [8, 8, 8], float64
def octonion_mul(a: torch.Tensor, b: torch.Tensor) -> torch.Tensor: ...
```

From src/octonion/_fano.py:
```python
FANO_PLANE: FanoPlane  # Singleton with .triples, .lines, .incidence_matrix, .quaternionic_subalgebra()
```

From tests/conftest.py:
```python
# Hypothesis strategies producing torch.Tensor of shape [8] at float64
def octonions() -> st.SearchStrategy[torch.Tensor]: ...
def unit_octonions() -> st.SearchStrategy[torch.Tensor]: ...
def nonzero_octonions() -> st.SearchStrategy[torch.Tensor]: ...
RTOL_FLOAT64 = 1e-12
ATOL_FLOAT64 = 1e-12
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Octonion class, R/C/H tower types, and random generators</name>
  <files>src/octonion/_octonion.py, src/octonion/_tower.py, src/octonion/_random.py, src/octonion/__init__.py, tests/conftest.py, tests/test_octonion_class.py, tests/test_types.py, tests/test_tower.py, tests/test_random.py</files>
  <behavior>
    - Test: Octonion(tensor) stores data, .components returns it, .real returns e0, .imag returns e1..e7
    - Test: o[i] returns component i for i in 0..7
    - Test: Octonion * Octonion delegates to octonion_mul and returns Octonion
    - Test: Octonion + Octonion is component-wise addition
    - Test: Octonion - Octonion is component-wise subtraction
    - Test: -Octonion negates all components
    - Test: scalar * Octonion and Octonion * scalar scale all components
    - Test: Octonion + scalar adds to real part only
    - Test: Octonion == Octonion compares components element-wise
    - Test: .conjugate() negates imaginary, preserves real
    - Test: .norm() returns sqrt of sum of squared components
    - Test: .inverse() returns conjugate/norm_squared, zero octonion raises ValueError with math context
    - Test: Octonion.__repr__ shows tensor form, __str__ shows symbolic form
    - Test: Octonion is immutable (no in-place mutation methods)
    - Test: Octonion rejects tensor with last dim != 8
    - Test: No __truediv__ or __pow__ operators exist (user decision)
    - Test: UnitOctonion has norm == 1 (within tolerance), constructed from normalized data
    - Test: PureOctonion has real part == 0
    - Test: Real, Complex, Quaternion implement NormedDivisionAlgebra correctly
    - Test: Quaternion multiplication matches Hamilton product for known values
    - Test: Complex multiplication matches standard complex arithmetic
    - Test: from_quaternion_pair(q1, q2) creates correct octonion
    - Test: to_quaternion_pair() and from_quaternion_pair() are inverses of each other
    - Test: random_octonion() produces [8] tensor, respects seed, dtype, device
    - Test: random_unit_octonion() produces norm-1 octonion
    - Test: random_pure_octonion() produces real-part-0 octonion
    - Test: random functions support batch_size parameter
  </behavior>
  <action>
1. Write all tests FIRST across the four test files.

2. Implement `src/octonion/_octonion.py`:
   - `class Octonion(NormedDivisionAlgebra)` with `__slots__ = ('_data',)`
   - `__init__(self, data: torch.Tensor)`: validate last dim is 8, store data
   - Properties: `components`, `real`, `imag`, `dim` (returns 8)
   - `__getitem__(self, i)`: return `self._data[..., i]`
   - Operators: `__mul__`, `__rmul__`, `__add__`, `__radd__`, `__sub__`, `__rsub__`, `__neg__`, `__eq__`
   - For `__mul__`: If other is Octonion, use `octonion_mul`. If scalar, scale components.
   - For `__add__`: If other is Octonion, add components. If scalar, add to real part.
   - NO `__truediv__` or `__pow__` per user decision (division ambiguity, parenthesization matters)
   - Methods: `conjugate()`, `norm()`, `inverse()` per RESEARCH.md Pattern 3
   - `inverse()` raises ValueError with math context message for zero octonion
   - `associator(a, b, c)` as module-level function: `(a*b)*c - a*(b*c)`
   - `from_quaternion_pair(q1, q2)` class method and `to_quaternion_pair()` instance method
   - `__repr__` showing tensor form, `__str__` showing symbolic form (e.g., "1.0 + 2.0*e1 + ...")
   - Truncated display for batched octonions (like PyTorch tensor printing)
   - `class UnitOctonion(Octonion)`: validates/enforces norm=1 at construction
   - `class PureOctonion(Octonion)`: validates/enforces real=0 at construction

3. Implement `src/octonion/_tower.py`:
   - `class Real(NormedDivisionAlgebra)`: dim=1, [..., 1] tensors, scalar multiplication
   - `class Complex(NormedDivisionAlgebra)`: dim=2, [..., 2] tensors, standard complex mul
   - `class Quaternion(NormedDivisionAlgebra)`: dim=4, [..., 4] tensors, Hamilton product
   - Each implements: `conjugate()`, `norm()`, `inverse()`, `__mul__()`, `components`, `dim`
   - Each has operator overloading: +, -, *, neg, eq, scalar interop

4. Implement `src/octonion/_random.py`:
   - `random_octonion(batch_size=None, dtype=torch.float64, device=None, generator=None)`: Gaussian components
   - `random_unit_octonion(...)`: Gaussian then normalize to unit norm
   - `random_pure_octonion(...)`: Set real component to 0
   - All support `batch_size` (None = single, int = batch), `generator` for seed control

5. Update `tests/conftest.py`:
   - Update strategies to produce `Octonion` instances (not raw tensors) now that the class exists
   - Keep raw tensor strategies available too (aliased as `octonion_tensors`, etc.)

6. Update `src/octonion/__init__.py` to export: `Octonion`, `UnitOctonion`, `PureOctonion`, `Real`, `Complex`, `Quaternion`, `NormedDivisionAlgebra`, `associator`, `random_octonion`, `random_unit_octonion`, `random_pure_octonion`, plus all Plan 01 exports.

7. Run tests -- all should PASS (GREEN phase).

IMPORTANT: Octonion is IMMUTABLE per user decision. All operations return new instances. No in-place mutation.
IMPORTANT: No __truediv__ or __pow__ per user decision.
IMPORTANT: Full scalar interop: scalar * octonion, octonion * scalar, octonion + scalar (adds to real part).
IMPORTANT: Verbose error messages with math context per user decision.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/research/octonion-computation-substrate && uv run pytest tests/test_octonion_class.py tests/test_types.py tests/test_tower.py tests/test_random.py -v --tb=short</automated>
  </verify>
  <done>Octonion class fully operational with all operators, UnitOctonion and PureOctonion subtypes work, R/C/H tower types implement NormedDivisionAlgebra, random generators produce correct distributions with seed control, all class-level tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FOUND-01 property-based test suite (Moufang, norm preservation, inverse, alternativity)</name>
  <files>tests/test_algebraic_properties.py</files>
  <behavior>
    - Test: Moufang identity 1: z(x(zy)) = ((zx)z)y for 10,000+ random triples within 1e-12
    - Test: Moufang identity 2: x(z(yz)) = ((xz)y)z for 10,000+ random triples within 1e-12
    - Test: Moufang identity 3: (zx)(yz) = (z(xy))z for 10,000+ random triples within 1e-12
    - Test: Flexibility: (xy)x = x(yx) for 10,000+ random triples within 1e-12
    - Test: Norm preservation |a*b| = |a|*|b| within 1e-12 relative error for random inputs
    - Test: Left inverse: a * a.inverse() = identity within numerical precision
    - Test: Right inverse: a.inverse() * a = identity within numerical precision
    - Test: Left alternativity: a*(a*b) = (a*a)*b within 1e-12
    - Test: Right alternativity: (b*a)*a = b*(a*a) within 1e-12
    - Test: Associator is zero when any two arguments are equal
    - Test: Associator is non-zero for generic random triples (magnitude is O(1) not O(epsilon))
    - Test: Associator is totally antisymmetric: [a,b,c] = -[b,a,c] = -[a,c,b]
    - Test: Precision tracking reports max/mean/std of relative errors (not just pass/fail)
  </behavior>
  <action>
1. Create `tests/test_algebraic_properties.py` with comprehensive property-based tests using Hypothesis:

2. Implement `check_moufang(a, b, c, tol)` utility following RESEARCH.md Pattern 5 exactly. This returns detailed error statistics (max_error, mean_error, std_error per identity).

3. Test functions (each using `@given` with the Hypothesis strategies from conftest):

   a. `test_moufang_identities`: Tests all 4 Moufang identities on random Octonion triples.
      - Use `@settings(max_examples=10000)` to meet the ROADMAP criterion of "10,000+ random triples"
      - Assert max error < 1e-12 for each identity
      - Print precision statistics (max/mean/std) on failure

   b. `test_norm_preservation`: |a*b| = |a|*|b| within 1e-12 relative error.
      - Relative error = |actual - expected| / expected where expected = |a|*|b|
      - Use `@settings(max_examples=10000)`

   c. `test_inverse_left`: a * a.inverse() has all components within 1e-12 of identity [1,0,0,0,0,0,0,0].
      - Use nonzero_octonions strategy

   d. `test_inverse_right`: a.inverse() * a has all components within 1e-12 of identity.
      - Use nonzero_octonions strategy

   e. `test_left_alternativity`: a*(a*b) = (a*a)*b within 1e-12.
      - This is the "associator zero when two args equal" for left case

   f. `test_right_alternativity`: (b*a)*a = b*(a*a) within 1e-12.

   g. `test_associator_zero_equal_args`: associator(a, a, b) = 0, associator(a, b, a) = 0, associator(a, b, b) = 0.

   h. `test_associator_nonzero_generic`: For random non-degenerate triples, ||[a,b,c]|| / (||a||*||b||*||c||) is O(1), confirming genuine non-associativity.

   i. `test_associator_antisymmetry`: [a,b,c] = -[b,a,c] and [a,b,c] = -[a,c,b].

4. Each test should capture and report precision statistics using a helper function `report_precision(errors, test_name)` that prints max/mean/std of relative errors per user decision.

5. Run the full suite with `--hypothesis-seed=0` to get deterministic results.

CRITICAL: The Moufang test must use `max_examples=10000` or higher. This is a ROADMAP success criterion.
CRITICAL: Tolerance is 1e-12 at float64. This is a locked decision.
CRITICAL: Tests report max/mean/std of errors per user decision -- not just pass/fail.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/research/octonion-computation-substrate && uv run pytest tests/test_algebraic_properties.py -v --tb=short --hypothesis-seed=0 -s 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 ROADMAP success criteria for Phase 1 now have passing tests: (1) Moufang on 10k+ triples, (2) norm preservation within 1e-12, (3) Cayley-Dickson cross-check (Plan 01), (4) inverse identity, (5) alternativity/associator. Precision statistics reported for each test.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v --hypothesis-seed=0` all green
- `uv run python -c "from octonion import Octonion, Real, Complex, Quaternion; print('tower ok')"` succeeds
- Moufang identities tested on 10,000+ triples (ROADMAP criterion 1)
- Norm preservation within 1e-12 (ROADMAP criterion 2)
- Inverse identity confirmed (ROADMAP criterion 4)
- Alternativity and non-zero associator confirmed (ROADMAP criterion 5)
</verification>

<success_criteria>
- Octonion class is feature-complete with all specified operators and methods
- R/C/H tower types all implement NormedDivisionAlgebra correctly
- All 5 ROADMAP Phase 1 success criteria have passing tests
- Random generators are reproducible with seed control
- Test precision statistics are reported (not just pass/fail)
</success_criteria>

<output>
After completion, create `.planning/phases/01-octonionic-algebra/01-02-SUMMARY.md`
</output>
