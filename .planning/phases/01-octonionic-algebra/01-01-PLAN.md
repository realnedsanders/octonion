---
phase: 01-octonionic-algebra
plan: 01
type: execute
wave: 2
depends_on: ["01-00"]
files_modified:
  - pyproject.toml
  - src/octonion/__init__.py
  - src/octonion/_types.py
  - src/octonion/_multiplication.py
  - src/octonion/_fano.py
  - src/octonion/_cayley_dickson.py
  - src/octonion/py.typed
  - tests/conftest.py
  - tests/test_multiplication.py
  - tests/test_cayley_dickson.py
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "Fano plane multiplication produces correct basis element products (e_i * e_j = e_k for all 7 oriented triples)"
    - "Cayley-Dickson recursive multiplication produces identical results to Fano plane structure constants multiplication on all 64 basis pairs"
    - "Structure constants tensor has exactly 50 non-zero entries out of 512"
    - "Project installs and pytest collects tests without errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project configuration with uv, PyTorch, pytest, hypothesis"
      contains: "octonion"
    - path: "src/octonion/_multiplication.py"
      provides: "Structure constants tensor and octonion_mul function"
      exports: ["STRUCTURE_CONSTANTS", "octonion_mul", "_build_structure_constants"]
      min_lines: 40
    - path: "src/octonion/_fano.py"
      provides: "FanoPlane data structure with triples, lines, incidence matrix, subalgebras"
      exports: ["FanoPlane", "FANO_PLANE"]
      min_lines: 50
    - path: "src/octonion/_cayley_dickson.py"
      provides: "Cayley-Dickson recursive multiplication as cross-check"
      exports: ["cayley_dickson_mul", "quaternion_mul", "quaternion_conj"]
      min_lines: 30
    - path: "src/octonion/_types.py"
      provides: "NormedDivisionAlgebra abstract base class"
      exports: ["NormedDivisionAlgebra"]
      min_lines: 30
    - path: "tests/conftest.py"
      provides: "Hypothesis strategies for octonions, tolerance constants"
      min_lines: 30
    - path: "tests/test_multiplication.py"
      provides: "Tests for Fano plane multiplication correctness"
      min_lines: 40
    - path: "tests/test_cayley_dickson.py"
      provides: "Cross-check test: Fano plane vs Cayley-Dickson"
      min_lines: 30
  key_links:
    - from: "src/octonion/_multiplication.py"
      to: "src/octonion/_fano.py"
      via: "Fano plane triples used to build structure constants"
      pattern: "triples.*\\(1.*2.*4\\)"
    - from: "tests/test_cayley_dickson.py"
      to: "src/octonion/_cayley_dickson.py"
      via: "Cross-check function imported and compared"
      pattern: "cayley_dickson_mul"
    - from: "tests/test_multiplication.py"
      to: "src/octonion/_multiplication.py"
      via: "octonion_mul tested on basis elements"
      pattern: "octonion_mul"
---

<objective>
Set up the octonion project from scratch and implement the core multiplication engine with dual implementations (Fano plane structure constants and Cayley-Dickson recursion), plus the test infrastructure to verify correctness.

Purpose: This is the foundation -- every subsequent plan depends on correct multiplication. The dual-implementation cross-check (success criterion 3 from ROADMAP) is built and tested here.
Output: Installable Python package with verified octonion multiplication, Fano plane data structure, Cayley-Dickson cross-check, and test infrastructure (conftest with Hypothesis strategies).
</objective>

<execution_context>
@/home/aescalera/.claude/get-shit-done/workflows/execute-plan.md
@/home/aescalera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-octonionic-algebra/01-CONTEXT.md
@.planning/phases/01-octonionic-algebra/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding and package setup</name>
  <files>pyproject.toml, src/octonion/__init__.py, src/octonion/py.typed, src/octonion/_types.py, tests/conftest.py</files>
  <action>
Create the project structure from scratch using uv:

1. Run `uv init --package octonion` or manually create pyproject.toml with:
   - Package name: `octonion`
   - Python: `>=3.12`
   - Build system: hatchling with src layout
   - Dependencies: `torch>=2.7` (with ROCm index URL if available, otherwise standard PyTorch)
   - Dev dependencies: `pytest`, `hypothesis>=6.0`, `hypothesis-torch`, `numpy>=1.26`, `ruff`, `mypy`
   - [tool.pytest.ini_options]: `testpaths = ["tests"]`, `addopts = "-x --tb=short"`, hypothesis profile settings
   - [tool.ruff]: sensible defaults (line-length 100, target Python 3.12)
   - [tool.mypy]: strict mode

2. Create `src/octonion/__init__.py` as a minimal entry point that will grow as modules are added. For now, export nothing (placeholder).

3. Create `src/octonion/py.typed` (empty PEP 561 marker file).

4. Create `src/octonion/_types.py` with the `NormedDivisionAlgebra` abstract base class:
   - Abstract methods: `conjugate()`, `norm()`, `inverse()`, `__mul__()`, `components` property, `dim` property
   - Concrete method: `norm_squared()` using `torch.sum(self.components ** 2, dim=-1)`
   - Follow the exact pattern from RESEARCH.md "NormedDivisionAlgebra Abstract Base Class" section

5. Create `tests/conftest.py` with:
   - Hypothesis strategies: `octonions()`, `unit_octonions()`, `nonzero_octonions()` as composite strategies
   - Tolerance constants: `RTOL_FLOAT64 = 1e-12`, `ATOL_FLOAT64 = 1e-12`
   - Hypothesis settings profile for CI: `max_examples=10000`, `deadline=None`
   - Hypothesis settings profile for dev: `max_examples=200`, `deadline=5000`
   - Register profiles and set default to "dev"
   - Strategies should produce raw `torch.Tensor` of shape `[8]` at float64 (not Octonion class -- that comes in Plan 02)

6. Run `uv sync` to install all dependencies and verify the environment works.
7. Run `uv run pytest --co -q` to verify test collection works (should collect 0 tests, no errors).

IMPORTANT: Use `uv` for all dependency management per user decision. Do NOT use pip directly.
IMPORTANT: float64 is the default precision per user decision.
IMPORTANT: If `uv init --package` creates files that conflict with the desired structure, adjust manually.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/research/octonion-computation-substrate && uv run python -c "import octonion; print('import ok')" && uv run pytest --co -q</automated>
  </verify>
  <done>pyproject.toml configured with all dependencies, src/octonion/ package importable, tests/conftest.py provides Hypothesis strategies and tolerance constants, `uv run pytest --co` succeeds with no errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core multiplication engine (Fano plane + Cayley-Dickson) with tests</name>
  <files>src/octonion/_fano.py, src/octonion/_multiplication.py, src/octonion/_cayley_dickson.py, src/octonion/__init__.py, tests/test_multiplication.py, tests/test_cayley_dickson.py</files>
  <behavior>
    - Test: e_i * e_0 = e_i and e_0 * e_i = e_i for all i (identity)
    - Test: e_i * e_i = -e_0 for all i in 1..7 (imaginary unit squaring)
    - Test: e_1 * e_2 = e_4, e_2 * e_1 = -e_4 (Baez 2002 convention check)
    - Test: All 7 Fano plane triple products are correct with cyclic and anti-cyclic signs
    - Test: Full 8x8 basis multiplication table has exactly 64 correct entries
    - Test: Structure constants tensor has shape [8,8,8] and exactly 50 non-zero entries
    - Test: Cayley-Dickson mul produces identical results to Fano plane mul on all 64 basis element pairs (FOUND-01 success criterion 3)
    - Test: Cayley-Dickson mul produces identical results to Fano plane mul on 100+ random octonion pairs (within 1e-12)
    - Test: Multiplication distributes over addition (property test)
    - Test: Multiplication is NOT associative for generic triples (non-associativity confirmation)
  </behavior>
  <action>
1. Write tests FIRST in `tests/test_multiplication.py`:
   - `test_identity_element`: e_0 acts as multiplicative identity from both sides
   - `test_imaginary_unit_squaring`: e_i^2 = -1 for i=1..7
   - `test_fano_triple_products`: All 7 triples produce correct products with correct signs (cyclic positive, anti-cyclic negative)
   - `test_full_basis_table`: Complete 8x8 multiplication table matches Baez 2002
   - `test_structure_constants_shape_and_sparsity`: Tensor is [8,8,8], has exactly 50 non-zero entries
   - `test_distributivity`: a*(b+c) = a*b + a*c for random octonions (property-based with Hypothesis)
   - `test_not_associative`: (a*b)*c != a*(b*c) for generic random triples

2. Write tests FIRST in `tests/test_cayley_dickson.py`:
   - `test_fano_cd_crosscheck_basis`: All 64 basis element products match between Fano and CD implementations
   - `test_fano_cd_crosscheck_random`: Random octonion pairs produce matching results (1e-12 tolerance)
   - `test_quaternion_mul_correctness`: The internal quaternion multiplication matches known Hamilton product results

3. Run tests -- all should FAIL (RED phase).

4. Implement `src/octonion/_fano.py`:
   - `FanoPlane` frozen dataclass with:
     - `triples`: The 7 oriented triples `(1,2,4), (2,3,5), (3,4,6), (4,5,7), (5,6,1), (6,7,2), (7,1,3)`
     - `lines` property: unoriented lines as frozensets
     - `incidence_matrix` property: 7x7 tensor
     - `quaternionic_subalgebra(line_index)`: returns the triple
     - `automorphism_generators` property: cycle-7 and quadratic residue permutations
   - Module-level `FANO_PLANE = FanoPlane()` singleton
   - Follow RESEARCH.md Pattern 5 (Complete Fano Plane Data Structure) exactly

5. Implement `src/octonion/_multiplication.py`:
   - `_build_structure_constants() -> torch.Tensor`: Build [8,8,8] structure constants from Fano plane triples
   - Module-level `STRUCTURE_CONSTANTS = _build_structure_constants()` (built once at import time)
   - `octonion_mul(a: torch.Tensor, b: torch.Tensor) -> torch.Tensor`: Multiplication via `torch.einsum('...i, ijk, ...j -> ...k', a, C, b)` with device/dtype handling
   - Follow RESEARCH.md Pattern 1 exactly for the structure constants build logic
   - Use the FANO_PLANE.triples to build the structure constants (don't duplicate the triple data)

6. Implement `src/octonion/_cayley_dickson.py`:
   - `quaternion_mul(p, q)`: Hamilton product for [..., 4] tensors
   - `quaternion_conj(q)`: Negate imaginary parts
   - `cayley_dickson_mul(x, y)`: Formula `(a,b)(c,d) = (ac - db*, a*d + cb)` per Baez 2002
   - Follow RESEARCH.md Pattern 2 exactly

7. Update `src/octonion/__init__.py` to export: `FANO_PLANE`, `FanoPlane`, `octonion_mul`, `STRUCTURE_CONSTANTS`, `cayley_dickson_mul`

8. Run tests -- all should PASS (GREEN phase).

CRITICAL: The Fano plane triples are (1,2,4), (2,3,5), (3,4,6), (4,5,7), (5,6,1), (6,7,2), (7,1,3). This is the Baez 2002 mod-7 convention. Do NOT mix with any other source.
CRITICAL: The Cayley-Dickson formula is (a,b)(c,d) = (ac - db*, a*d + cb) per Baez 2002. Do NOT use the Wikipedia formula.
CRITICAL: If the Cayley-Dickson cross-check fails on basis elements, this means the quaternion-pair split does not directly correspond to the Fano plane convention. In that case, determine the correct permutation mapping and document it. This IS the research question from Open Question 1 in RESEARCH.md.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/ai/research/octonion-computation-substrate && uv run pytest tests/test_multiplication.py tests/test_cayley_dickson.py -v --tb=short</automated>
  </verify>
  <done>All multiplication tests pass: basis element products match Baez 2002, structure constants have correct shape and sparsity, Cayley-Dickson cross-check confirms identical results to Fano plane implementation on all 64 basis pairs and random inputs, distributivity holds, non-associativity confirmed for generic triples</done>
</task>

</tasks>

<verification>
- `uv run python -c "import octonion; print('package ok')"` succeeds
- `uv run pytest tests/test_multiplication.py tests/test_cayley_dickson.py -v` all green
- Structure constants tensor has shape [8, 8, 8] with exactly 50 non-zero entries
- `e_1 * e_2 = e_4` confirmed (Baez 2002 convention)
- Cayley-Dickson matches Fano plane on all 64 basis pairs (FOUND-01 success criterion 3)
</verification>

<success_criteria>
- Installable package via `uv run python -c "from octonion import octonion_mul"`
- All basis element multiplication products match Baez 2002 convention
- Cayley-Dickson cross-check passes on basis elements AND random inputs
- Test infrastructure (conftest strategies, tolerance constants) ready for downstream plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-octonionic-algebra/01-01-SUMMARY.md`
</output>
